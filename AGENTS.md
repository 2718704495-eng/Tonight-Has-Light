# 《今夜有灯》项目协作规则

本文件是项目内所有 Codex 任务的共享记忆与强制执行入口。开始需求、UI、美术、动效、Cocos 或 QA 工作前，先读取本文件；详细当前状态见 `docs/PROJECT-MEMORY.md`。

## 负责人和分工

- 主任务窗口是唯一总负责人：确认最新需求、分派任务、合并结论、控制 Gate，并向用户汇报。
- 需求与体验任务维护用户旅程、需求编号和变更影响，不直接替 UI 或开发作最终决定。
- UI 与美术任务负责可见母版、角色同框、分层资产和动效故事板；未通过视觉 Gate 不交给开发量产。
- Cocos 任务负责技术可行性、持久场景、资源契约、性能和正式实现；不得用临时 Graphics 图形冒充最终 UI。
- QA 任务独立检查需求—设计—代码一致性、真机、无障碍、性能和恢复；自动测试通过不等于视觉 Gate 通过。
- QA／审查任务始终只读，即使用户已向主任务授权上传或其他远端写操作，也不得代为执行。所有微信上传、体验版设置、提审、发布和远端资源修改只允许主任务单点执行；子任务只能准备命令、校验输入和审查回执。
- 微信上传必须单飞并绑定最终构建：所有源码、配置和文档修正结束后，主任务重新同步已批准 AppID，运行 experience/release 守卫与包体审计，冻结当前构建树哈希，再执行 CLI upload。上传后立即复算同一 buildRoot 的关键哈希并把脱敏原始输出、版本与构建树绑定；CLI exit `0` 或表面 `✔ upload` 不能单独作为成功依据，还须确认微信工具完成远端提交链（例如上传令牌、`startCosUpload`、`commitTask`，或等价的远端版本查询）。若发现网络 false positive 或并发任务提前上传同版本旧候选，旧回执必须标为 `SUPERSEDED`，不得用同版本号或相近包体冒充最终候选。
- 各任务不回退或覆盖其他任务的修改；出现冲突时报告给主任务，由主任务裁决。

## 真相源优先级

冲突时按以下顺序执行：

1. 用户在主任务窗口里的最新明确决定。
2. 本文件与 `docs/PROJECT-MEMORY.md` 中标记为“当前锁定”的内容。
3. 当前 Gate 的已批准母版、角色圣经、资产清单和验收证据。
4. `docs/SCENE-01-NIGHT-BREEZE-BRIEF.md`、`docs/OUTDOOR-ENTRY-REDESIGN-V4.md`、角色/五夜/资产/质量文档的未冲突部分。
5. 旧浏览器原型、旧室内 Cocos V0、探索图和历史交付说明。

旧材料只能作为研究或回归参照，不能覆盖新决定。发现分叉时先标记 `DRIFT`，不要默默选择其中一版继续开发。

## 当前不可改变的产品目标

- 面向疲惫下班后的用户，提供 3–8 分钟、无失败、无打卡、无奖励压力的夜间过渡体验；户外赏景可无限停留并可随时离开。
- 第一场景是“自然星空下的夜风草坡”，不是立即进入室内，也不是通往小屋的过场。
- 当前角色锁定为“一位性别表达克制的卡通成年人背影＋一只普通家猫”，肩并肩和用户一起看夜空；小屋在右侧中景，门从第一秒可点，但不闪烁、不催促。
- 夜空以用户批准的 V7 为方向：普通深蓝星空占主体，只有一条宽而淡、边缘松散、带暗纹和断口的自然银河；禁止发光水彩飘带、星座连线和满屏特效，仅 8–10 颗主星错峰微亮。
- 两朵小花只发很弱的微光；风按远草→近草→人物发梢/衣角→猫耳/尾尖传递，方向一致、节奏不同步。
- 零操作也必须有完整可观看内容；花、天空、慢滑生风、门均为可选互动，不产生任务、奖励或进度压力。
- 人物不公开姓名、性别或具体关系；猫保持普通家猫比例、不穿衣、不拟人。正式人物服装剪影、猫体态与两者间距必须原创重绘，不照搬现成卡通 IP。
- 生成图默认只用于构图、光色和材质探索。唯一当前例外是用户已批准的 `FORMAL-PICTUREBOOK-FULLFRAME-V1-A`：该规格内逐页审核的无字全幅 clean plate 可登记为 `ai-assisted-formal-fullframe` 正式候选；每页仍须完整提示词、来源、哈希、相似性检查、负责人／独立审查和用户同一文件批准。该例外不扩展到角色单体、中文、Logo、图标、UI、流星或其他关键互动物，它们仍必须独立可编辑、可追溯。
- 首次触碰前用画面表达风；首次触碰后才播放声音，环境风先响应，音乐在 2–3 秒内渐入。静音仍可理解全部内容。
- 用户主动进屋后的室内必须与户外形成情绪差：户外安静、清凉、舒心；室内像深夜回家发现灯一直亮着，整屋明亮温暖、墙顶地家具和角色可读、角落不能黑。餐桌、壶杯和家的秩序已经在等待，人物会在 `HOME-MEAL-RITUAL-V1-A` 中放下外衣、去厨房完成一份简单热食，再回到饭桌。用户已批准 `FORMAL-UI-V1.2-A / 灯一直为你亮着`：门内从第一帧就是稳定明亮暖家，不播放黑房亮灯表演；B“进门后整屋亮起”未获批准。第一版不自动加入群像、派对或具体关系。

## Gate 顺序

1. **需求锁定**：当前合同、被替代项、需求追踪表和待决问题完整。
2. **静态视觉母版**：390×844 V7 方向图、角色同框、材质与受光统一，通过主任务和用户确认。V7 只批准进入可丢弃动态样片，不因此变成正式入包资产。
3. **动态样片**：在 Cocos 中完成 8–10 秒真实外景样片，至少验证一次风传递、异步角色呼吸、主星错峰、花光和减动等价态。
4. **正式开发**：前三项通过后，才接入完整户外交互、进门转场和室内五夜。

没有证据不得把 Gate 标成通过。旧室内 V0 的 `npm run verify` 只证明旧逻辑回归，不证明新户外视觉通过。

## Skill 路由

- 任何需求、视觉、动效、实现或验收变更：必须先用项目 Skill `tonight-design-gate`。
- 故事化插画／漫画改版、镜头节拍、跨画角色与场景连续性、定点修图提示词：使用项目 Skill `storyboard-skill`；它负责分镜合同与连续性，不替代 `frontend-design` 的最终视觉判断。
- 新母版、角色同框、色彩、字体和独特视觉方向：用 `frontend-design`，再用 `ui-ux-pro-max` 检查触控、对比度、适配和一致性。
- 游戏内提示、设置、分享、暂停或 HUD：用 `game-ui-frontend`，并以“不遮挡赏景、不像仪表盘”为第一约束。
- 截图回归、触控路径和浏览器/Cocos Web 冒烟：用 `game-playtest`；重视觉状态必须有截图证据。
- 动效节奏与重量：按需使用全局 `ecc:motion-foundations` 或 `ecc:motion-advanced`；动效不得掩盖未通过的静态构图。
- 无障碍专项：按需使用全局 `ecc:accessibility`，同时保留 44×44 热区、减少动态、大字和静音等价路径。
- `imagegen` 默认只作探索；只有 `FORMAL-PICTUREBOOK-FULLFRAME-V1-A` 明确列出的全幅页，可按其批次和逐页 Gate 例外进入正式候选，不能自动成为运行时资产。

Skill 提供方法，不拥有产品决策权。它与本文件冲突时，以本文件和用户最新决定为准。

## 开发与验收停止线

- 禁止继续扩展 `TonightHasLightV0View` 的临时 Graphics 画面作为正式户外场景；正式表现使用持久 Scene/Prefab/Sprite/Atlas，状态变化不整屏销毁重建。
- 任何 AI 辅助正式分层在批量抽层前，必须先用一个高风险角色或纹理层做真实透明探针，并读取文件元数据确认 `RGBA / hasAlpha=true`、非目标区确为 Alpha 0、没有绘制棋盘格或背景幽灵。探针失败立即停止；不得用自动抠图、色键、整图底层或空层把失败包装成“真分层”。
- 户外行为使用独立应用流，不能误写入室内夜晚完成进度；点击门后再进入现有室内夜晚状态机。
- 360、390、430 宽度无裁切，触控区至少 44×44，相邻热区至少留 8px；大字为真实 120%，不能靠 SHRINK 假装适配。
- 减少动态时关闭位移、视差、草浪和镜头推进，保留不超过 200ms 的亮度或交叉淡化等价反馈。
- 中高端目标 55–60fps，低端稳定不低于 30fps；输入 P95 不超过 100ms；不得用满屏实时噪声、Bloom、3D 灯光或大量粒子堆效果。
- 不采集姓名、头像、自由留言或关系数据；不做医疗、助眠或心理治疗承诺。
- 构建体验版可以在用户授权范围内执行；上传体验版、提交审核、发布、推送 GitHub 或修改远程资源必须单独获得明确授权。

## 变更记录要求

每个会改变用户体验的决定都要在 `docs/PROJECT-MEMORY.md` 记录：日期、决定、替代项、影响的需求/设计/代码/测试、负责人和状态。交付时必须给出需求—视觉—代码—证据追踪表，状态只允许 `ALIGNED`、`DRIFT`、`BLOCKED`、`PASS`、`FAIL`。

## 视觉样式变更控制

- 角色、构图、材质、配色、夜空结构、光线层级和标志性动效均属于用户可见的样式决策；任何任务窗口不得自行改变。
- 主任务提出样式修改前必须先查阅可信参考，说明现状问题、研究结论、拟改内容、保持不变项、成本/性能/原创风险和受影响 Gate。
- 主任务负责形成带版本号的提示词或人工绘制说明；需要比较时先交 A/B 方案或对照板，再向用户汇报。用户明确批准后，才可把同一版本提示词分派给 UI/美术、开发与 QA。
- 未批准的样式提案状态一律为 `BLOCKED`，只能做只读研究和不进入构建的探索；不得覆盖已批准母版、修改运行时画风或把探索图交给开发。
- 实现中的像素修正、图层对齐和性能降级若不改变观感，可在既定红线内执行，但必须在交付记录中说明；一旦可能改变观感，立即回到主任务请用户裁决。
- 每次批准都要记录批准文件、版本、SHA-256、批准范围和被替代版本；工作窗口只能消费该记录指向的版本。
