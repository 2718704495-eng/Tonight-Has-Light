# HOME-MEAL-QUIET-UI-V1-A 所有者复核

状态：`PASS / LOCAL GATE B UI / USER VISUAL APPROVAL PENDING`。

## 输入身份

- `none`：`bbb02106fb4f5a820d1199eb61c7b10b5065028461bfdd6bf813276abe796533`
- `ate`：`0a356ea97cc91afecc93c8c9a607583d63a40db0a6e0226ec224be4a47a450b5`
- `sipped`：`710a4b4f54641e0880639147807120594b7e43302e74ce5d97c4784614cf841e`
- `both`：`69a9ab2b2982cd83beec6430c9684940f2b9924c5a15beb29615eeaaf722a530`
- response report：`f2b1642da1d405dc90802c45580318c1728775b9a939e680a450a80aef3fa752`
- response board：`ac3d68a7e40b6e946eb5895c6363ba8ff18a171a713f3628a99e3abc288e77ef`

构建器会先验证以上输入，任一哈希漂移即失败，不会覆盖最终 UI 证据。

## 可见复核

- H1 `放下外衣` 位于挂钩旁，不遮人物与猫；H2 两行提示位于人物右侧空地，没有任务卡感。
- H4 `吃一点 / 喝口温水` 等权、局部、安静；标准模式没有托盘、勾选、进度或奖励。
- H4 四状态在 100% 与 195×422 对照板中无新矩形补丁、白杯、重复筷子或桌纹断层。
- 120% 为真实 `19.2px / 16.8px`，未 SHRINK；H4 仅大字模式使用桌边不规则暖纸。
- 减动证据为静态 `both` 状态，位移、缩放、旋转均为零，只允许 `150ms` 交叉淡化。
- 360×800、390×844、430×932、430×844-pressure 无文字裁切或对象脱锚。压力尺寸两侧保留批准的深蓝安全边，不横向拉伸。

## 测量

- 实际渲染像素最低文字对比度：`10.84:1`，要求 `4.5:1`。
- 最小逻辑热区：`83×170px`；360 宽缩放后最小约 `76.62×156.92px`，要求不小于 `44×44px`。
- 两 H4 热区逻辑间距 `9px`；360 宽缩放后约 `8.31px`，要求至少 `8px`。
- 按下反馈 `120ms`、无位移；状态交叉淡化 `160ms`，不超过 `180ms`。

## 缺陷与边界

- 当前 P0：0；P1：0；P2：0。
- 早期响应层的自动测试曾通过，但画面出现补丁、白杯或重复筷子；UI 所有者拦截并将对应证据标为 superseded，未把机械 PASS 当视觉 PASS。
- 本复核不覆盖 Cocos 运行时、真机触控、微信包、性能或用户最终视觉批准；这些仍需后续 Gate 单独验证。
