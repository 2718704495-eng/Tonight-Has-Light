# HOME-MEAL-QUIET-UI-V1-A 证据状态

当前状态：`PASS / UI OWNER VISUAL+MECHANICAL REVIEW / USER VISUAL APPROVAL PENDING`。

- H1：稳定 500ms 后在左墙挂钩旁显示 `放下外衣`；暖白字＋深墨细描边，不加卡片。
- H2：停留 2500ms 后最多一次显示 `轻轻碰一下，故事会往前走。`；最终实图中暖白字＋深墨细描边已经可读，因此不启用暖纸，不形成提示卡。只有运行时底色变化破坏对比度时，才允许回退到局部不规则暖纸。
- H4：稳定 300ms 后，菜旁 `吃一点` 与杯旁 `喝口温水` 等权显示；无勾选、进度、奖励或完成语义。空白点击仍翻到 H5，标签只属于各自对象热区，不形成第三层任务卡。
- 字号：动作 `16px`、辅助 `14px`；大字为真实 `19.2px / 16.8px`，禁止 `SHRINK`。
- 反馈：按下只在 `120ms` 内改变透明度，布局和位置不动；对象状态使用 `160ms` 静态交叉淡化，减动为 `150ms` 且位移、缩放、旋转均为零。
- 几何：H4 两热区为 `146×170` 与 `83×170`，逻辑边缘间距 `9px`；360 宽 SHOW_ALL 后最窄目标仍为 `76.62px`，间距仍为 `8.31px`。其余目标也显著大于 `44×44`。

H4 的早期 `ate / sipped / both` 曾出现实心棕色刷痕、突兀浅色杯、重复筷子和桌纹擦除，本 UI 支线逐轮拦截，旧合成全部标记为 `SUPERSEDED / DO NOT CITE`。这避免了“自动测试通过但画面失败”。

本轮唯一输入为：`none bbb02106…6533`、`ate 0a356ea9…50b5`、`sipped 710a4b4f…841e`、`both 69a9ab2b…a530`。完整哈希写入 `home-meal-ui-contract.json` 与 `build-report.json`；构建脚本在输入漂移时直接失败。最终四态标准对照板、真实 120% 大字、减动静态态和四尺寸证据已重生成。UI 所有者在 100% 与缩略图复核中未见新的矩形补丁、白杯、重复筷子、文字裁切或任务托盘感；实际渲染像素最低对比度为 `10.84:1`。

本结论只覆盖本目录的本地 Gate B UI 源和证据，不代表 Cocos 运行时、真机触控或用户视觉批准。
