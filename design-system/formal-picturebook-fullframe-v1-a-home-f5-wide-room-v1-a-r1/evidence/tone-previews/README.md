# Tone previews

These are deterministic gamma previews derived from the same locked master. They are engineering checks for the approved “whole room is bright and readable” constraint, not additional generated artwork and not user-approved candidates.

The current frozen candidate remains `a-current` unless the project lead explicitly revises the normalization record and reruns independent review before showing the user.
