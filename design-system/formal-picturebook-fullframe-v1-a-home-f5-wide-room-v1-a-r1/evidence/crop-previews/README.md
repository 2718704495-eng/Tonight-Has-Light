# Crop previews

These three files are deterministic crops of the same single ImageGen result. They are engineering comparison artifacts, not three generated candidates and not user-approved art.

The frozen candidate uses `b-threshold-thin`: it preserves the pulled-back room while reducing the heavy dark entry threshold to a thin contextual edge. No pixels were generated, repainted, inpainted, or copied during this normalization.
