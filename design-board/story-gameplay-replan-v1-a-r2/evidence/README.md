# R2 browser visible-proof evidence

Status: `READY FOR FINAL HUMAN REVIEW` for the disposable browser candidate only. This is not a formal-art, Cocos, build, WeChat, or release acceptance.

## Final checkpoint after the second independent review

- The second review's four Important findings were addressed: replay-safe meteor routing now retains the legal `WIDE_SKY → METEOR_EVENT → AFTER_METEOR` edges; the approved quiet/streak/tail/quiet/copy timing is explicit; H5 includes cat, coat and meal consequences; and internal state/reviewer labels no longer leak into phone alt text.
- A browser reload race found during the final smoke pass was reproduced, protected by a focused RED test, and fixed with a pre-initialization render guard.
- Final automated verification is `17/17 PASS` across the contract and visible-proof suites.
- A clean browser pass reached Root, distinct F2, the full sky route, `WIDE_SKY`, a first `METEOR_EVENT` with exactly one meteor, hidden event copy and a full-screen tap equivalent, followed by the `AFTER_METEOR` choice state. No initialization error occurred after the guard fix.
- The request to create a Git handoff interrupted the remaining final pass. Therefore the older full-route notes below remain useful regression history, but the final revised candidate still needs both complete routes plus reduced-motion/large-text human review before any R2 visual PASS.

## Local browser smoke pass — repair candidate

- Served from the project root with `python3 -m http.server 4173` and opened at `/design-board/story-gameplay-replan-v1-a-r2/` in the Codex in-app browser.
- Root loaded after the quiet pause with the three in-art invitations; the phone line remained `今夜有灯` without a node counter or completion language.
- Root had exactly one each of `看看星空` / `吹吹风` / `回家` before and after settings re-render. Setting labels changed to `开`; computed copy scale was `1.2` and heading changed from 18px to 21.6px.
- Sky route reached the independent F2 state. The visible phone copy was exactly `今夜 · 银河深处` / `眼睛慢慢看清了` / `起初只有一片深蓝。再停一会儿，银河里的暗裂才慢慢分开。`. The transparent child zone covered 100% width × 72% height with `pointer-events:auto`; after its light-tap reveal the veil was gone and exactly one `继续看云` remained. The 720ms hold path is protected by the same child-zone timer contract and Node test; browser automation exercised the tap equivalent.
- The first outdoor pass showed exactly one `.meteor.run`. After choosing `再坐一会儿`, replaying the sky sequence changed the final invitation to `回到流星之后`; it reached `AFTER_METEOR` with `.meteor.run` count `0`.
- The primary route reached independent `KITCHEN_CALL` on H2 after the coat action. It showed the required warm-paper copy. A kitchen response created the visible steam class synchronously; after approximately 620ms it had transitioned to approved H3 (the specified delay is 560ms).
- The outdoor route's H5 evidence read `流星 · 从草坡看见`; after eating and drinking it retained `饭桌 · 吃过一点` and `温水 · 喝过一口`. Choosing `留一盏灯` changed the heading/body, updated the marker to `灯 · 留在门廊`, then revealed `今晚到这里` / `回到草坡`. The terminal view retained all markers and showed `不用完成更多。今晚已经被好好接住。`.
- A separate direct-home pass reached H5 with `流星 · 从窗边补足`, `饭桌 · 没有勉强多吃`, and `温水 · 杯子还暖着`.
- Browser console: no warning or error entries during either route.

## Scope and motion boundary

All CSS meteor, veil, steam, cloud/grass implication and temporary audio are `prototype-only`. Reduced motion uses only opacity/brightness/crossfade at no more than 180ms for the temporary effects; it removes displacement and warm expansion. No new image was generated or promoted.

## Hash anchors

- R1 browser proof: `760a0cba95d715c97cc3814b58be7206bf89727bb221036fec7e39f0fe554836` (rechecked unchanged).
- Task 1 R2 session manifest: `36ad74c9e8bf3ba5378f3746fe84213a0fa08992494b8bae88eddcf283a6cdcc` at Task 1 completion.
