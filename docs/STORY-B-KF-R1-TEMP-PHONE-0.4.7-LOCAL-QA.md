# STORY-B-KF-R1-TEMP 0.4.7 本地 QA

> 候选：`gate-d-story-b-kf-r1-temp-dev-r1-0.4.7`  
> 日期：2026-08-29  
> 结论：`LOCAL WEB VISUAL PASS / WECHAT BUILD PASS / PHONE HUMAN BLOCKED`

## 已验证

- 三拍严格为 `B01 3200ms → 300ms 转场 → B02 1500ms → 360ms 转场 → B03 无限停留`；B03 不循环、不自动进屋。
- 两个持久 Sprite 与 Graphics Stencil 转场无黑帧、白帧或透明空帧；墨带为半透明深靛渐变，不再遮成实心色块。
- 门从 B01、B02、B03 和两次转场均可用；后台打断门转场后回滚并重播，不会让再次点门失效。
- 减少动态固定 B01，不运行裁切、墨带或位移；首触前音频静默，首触后沿用已批准环境风。
- 360×800、390×844、430×932 与 430×844 压力态保持人物、猫、小屋、门和两朵花；安全边恢复已批准 `#06265F`。
- 点门进入整屋明亮暖屋；既有时长选择、壶盖、杯子、收尾和返回户外链保持。
- Cocos `mask` 模块已启用；旧 R2 页面运行类不再被当前 Cocos assets 自动编译或引用。
- `npm run verify`：`173/173 PASS`。
- Experience validator：`PASS`；Release validator：按设计因临时资产返回非零。
- 微信包审计：主包 `3,935,598 / 4,194,304 bytes`；总包 `13,496,497 / 20,971,520 bytes`；`PASS`。

## 独立只读审查

- 行为／构建审查：无剩余 P0/P1；P2 为转场点门可能出现极短单帧切换、未做长时间 Bundle 回收 soak、仍缺微信真机录像。
- 视觉审查：`LOCAL WEB VISUAL PASS`；B 风格、三拍角色和场景、暖门、两花、明亮暖屋均无未批准样式漂移。
- 视觉审查指出的 B02→B03 中点缺口已补：运行时为 `to-afterwind`，进度约 `0.666`，Mask 启用，两帧门区并集有效。

## 仍待手机验证

- 用户将开发版本 `0.4.7` 设为体验版后的真实微信原速观感。
- 两次转场中点门、B03 点门、减动固定 B01、首次触碰风声。
- 20% OLED / 60% LCD 暗部与安全区；前后台、锁屏、来电、耳机切换。
- 10 分钟内存、30 分钟户外停留和资源释放 soak。

这些缺口不阻止内部开发体验上传，但阻止把 `0.4.7` 写成正式 Gate D、审核或发布通过。
